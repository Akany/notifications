import React from 'react';

const styles = {
	textAlign: 'center'
};

export default class Header extends React.Component {
	render() {
		return (
			<header style={styles}>Build Your Notifications</header>
		);
	}
}